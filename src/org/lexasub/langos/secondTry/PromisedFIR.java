package org.lexasub.langos.secondTry;

import org.lexasub.langos.secondTry.utils.Promise;

import java.util.function.Function;
import java.util.stream.Stream;

public class PromisedFIR {
    public Promise promiseId(String text) {
    }

    public Promise promiseFuncArgs(Stream<Promise> promisedFIRStream, Stream<Promise> objectStream) {
    }

    public Promise promiseFunction(Object visitFunction_specifier, Promise visitType_name, Object visitNamspce_obj, Function visitFun_name, Promise visitFunc_args, Stream<Promise> visitBraced_element) {
    }

    public Object declareNamespace(Stream<Promise> promisedFIRStream) {
    }

    public Promise promiseMethodCall(Object visitNamspce_obj, Promise visitClass_name, Object visitFunction_call) {
    }

    public Promise promiseFunctionCall(Function visitFun_name, Stream<Promise> visitParened_expr_list) {
    }

    public Promise promiseFunctionCall_(Promise visitMethod_call, Promise visitFunction_call, Stream<Promise> promiseStream) {
    }

    public Promise promiseGetMember(Promise visitId, Promise visitMember_name) {
    }

    public Promise promiseLambda(Promise visitParened_id_list, Stream<Promise> visitBraced_element, Promise visitExpr) {
    }

    public Promise promiseProgram(Stream<Object> objectStream, Stream<Promise> promiseStream) {
    }

    public Promise promiseIds(Stream<Promise> promiseStream) {
    }

    public Promise promiseImport(Stream<Promise> promiseStream) {
    }

    public Promise promiseReturn(Promise visitExpr) {
    }

    public Promise promiseBreak() {
    }

    public Promise promiseContinue() {
    }
}
