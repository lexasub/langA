int gcd(int a, int b) {
  return if(
    () -> isZero(b),
    () -> a,
    () -> gcd(b, mod(a, b))
  )
  return f()
}

int gcd2(int v, int k) {
  while(
    () -> not(isZero(k)),
    () -> {
        set(v, mod(v, k))
       swap(v,k)
    }
  )
}
