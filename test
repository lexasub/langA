int gcd(int a, int b) {
  return if(
    () -> isZero(b),
    () -> a,
    () -> gcd(b, mod(a, b))
  )
  return f()
}

int gcd2(int a, int b) {
  while(
    () -> not(isZero(b)),
    () -> {
        set(a, mod(a, b))
       swap(a,b)
    }
  )
}
