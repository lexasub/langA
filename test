int gcd(int a, int b) {
  return if(
    () -> isZero(b),
    () -> a,
    () -> gcd(b, mod(a, b))
  )
  return f()
}

int gcd2(int a, int b) {
  while(
    () -> not(isZero(b)),
    () -> {
        set(a, mod(a, b))
       swap(a,b)
    }
  )
  return a;
  a.r((i) -> {return (k)->{return plus(k, i)}})
  map((l) -> map(a, (i) -> {return i}), (u) -> u)
}

int main() {
    return map((i)->i(), (j) -> j())
}