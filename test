int gcd2(int v, int k) {
  while(
    () -> not(isZero(k)),
    () -> {
       def(v0, phi (v1, v))
       def(v1, phi (t0, k))

       def(t0, mod(v0, v1))
    }
  )
  return v1
}
int gcd(int a, int b) {
  if(
    () -> isZero(b),
    () -> return a,
    () -> return gcd(b, mod(a, b))
  )
}