int gcd(int a, int b) {
  return if(
    () -> b.isZero(),
    () -> a,
    () -> gcd(b, a.mod(b))
  )
  f()
}

int gcd2(int a, int b) {
  while(
    () -> b.isZero().not(),
    () -> {
        set(a, a.mod(b))
       swap(a,b)
    }
  )
  return a;
}

